輸入檔名(ex : input1.txt)後(記得執行檔要和文件放在同個目錄下)，程式就會對該文件進行霍夫曼編碼。

題目是說英文字母、空格及標點符號要編碼，但因為我認為標點符號的涵蓋範圍很廣，所以我的程式設計成，只要是不是數字的可視字元都會進行編碼。

輸出時，會輸出各個字元的霍夫曼編碼以及四捨五⼊⾄小數第⼆位的壓縮率。

我在輸入輸出的地方設成了無限迴圈 (在Line245), 可以一直重複測試, 測完了直接關掉執行檔即可。